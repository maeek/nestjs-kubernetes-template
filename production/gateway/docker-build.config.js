/* eslint-disable */

module.exports = {
  // You can provide absolute or relative path to your Dockerfile
  // The relative path is resolved from the service folder
  dockerfilePath: '../../deploy/docker/Dockerfile',
  // dockerfilePath: path.resolve(config.basePath, config.servicesPath, 'gateway/Dockerfile')
};
